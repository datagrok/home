# The default distribution is "testing" (specified in /etc/apt/apt.conf), but
# we grab some packages from "unstable" so we list that here too.

# MIT's is a good, close, reliable debian mirror
deb http://debian.lcs.mit.edu/debian/ testing main contrib non-free
deb http://debian.lcs.mit.edu/debian/ unstable main contrib non-free

# Fall back to the official, slow site.
# deb http://ftp.us.debian.org/debian/ testing main contrib non-free
